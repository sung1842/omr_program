# Form wizard UI

Read `docs/form-wizard/architecture.md` and `docs/form-wizard/TASKS.md` before editing.

- Konva stays in `WizardCanvas` (image, region boxes, circles).
- Shell (steps, panels, buttons) stays in `FormWizard` and step files. Restyle later from a user-supplied 21st.dev (or similar) prompt — do not replace Konva.
- Owner split: canvas files vs `FormWizard.tsx` / `app/(app)/templates/new/page.tsx` as in TASKS.md.
- Do not push to GitHub.
